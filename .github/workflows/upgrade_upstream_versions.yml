name: Version Update

# Triggered by anitya webhook (repository_dispatch) or manual workflow_dispatch.
# 1. Query anitya for apps with new upstream versions
# 2. For each app: call _upgrade_versions.yml (build + test)
# 3. If verify fails: run fixer, re-verify (up to 3 rounds)
# 4. Create PR for successfully verified apps

on:
  repository_dispatch:
    types: [anitya-version-update]
  workflow_dispatch:
    inputs:
      app:
        description: 'Specific app to update (blank = all)'
        required: false

env:
  PYTHONUNBUFFERED: "1"

jobs:
  query:
    name: Query new upstream versions
    runs-on: [self-hosted, Linux, x64]
    outputs:
      updates: ${{ steps.q.outputs.updates }}
    steps:
      - name: Check out workflow source
        uses: actions/checkout@v4
      - id: q
        name: Query new upstream versions
        run: |
          UPDATES=$(python scripts/harness/query_version.py \
            ${%-%github.event.inputs.app%} --format json)
          echo "updates=$UPDATES" >> "$GITHUB_OUTPUT"

  # --- Per-app verify + fix ---
  round-1:
    name: "Round 1: verify images"
    needs: query
    if: needs.query.outputs.updates != '[]'
    strategy:
      fail-fast: false
      max-parallel: ${{ vars.MAX_PARALLEL_IMAGES || 4 }}
      matrix:
        app: ${{ fromJSON(needs.query.outputs.updates) }}
    uses: ./.github/workflows/_upgrade_versions.yml
    with:
      app: ${{ matrix.app.name }}
      app_version: ${{ matrix.app.version }}
      os_version: ${{ matrix.app.os_version }}
      scenario: version-update

  fix-1:
    name: "Fix failures (round 1)"
    needs: round-1
    if: failure()
    strategy:
      fail-fast: false
      max-parallel: ${{ vars.MAX_PARALLEL_IMAGES || 4 }}
      matrix:
        app: ${{ fromJSON(needs.query.outputs.updates) }}
    runs-on: [self-hosted, Linux, x64]
    steps:
      - name: Check out workflow source
        uses: actions/checkout@v4
      - name: Install opencode
        run: curl -fsSL https://opencode.ai/install | bash
      - name: Download test results
        uses: actions/download-artifact@v4
        with:
          pattern: test-results-${{ matrix.app.name }}-*
          merge-multiple: true
      - name: Fix build failures
        env:
          ANTHROPIC_BASE_URL: https://api.deepseek.com/anthropic
          ANTHROPIC_AUTH_TOKEN: ${{ secrets.DEEPSEEK_API_KEY }}
        run: python scripts/harness/run.py fix

  round-2:
    name: "Round 2: verify images"
    needs: fix-1
    if: always() && !failure()
    strategy:
      fail-fast: false
      max-parallel: ${{ vars.MAX_PARALLEL_IMAGES || 4 }}
      matrix:
        app: ${{ fromJSON(needs.query.outputs.updates) }}
    uses: ./.github/workflows/_upgrade_versions.yml
    with:
      app: ${{ matrix.app.name }}
      app_version: ${{ matrix.app.version }}
      os_version: ${{ matrix.app.os_version }}
      scenario: version-update
      retry: true

  fix-2:
    name: "Fix failures (round 2)"
    needs: round-2
    if: failure()
    strategy:
      matrix:
        app: ${{ fromJSON(needs.query.outputs.updates) }}
    runs-on: [self-hosted, Linux, x64]
    steps:
      - name: Check out workflow source
        uses: actions/checkout@v4
      - name: Fix build failures
        env:
          ANTHROPIC_BASE_URL: https://api.deepseek.com/anthropic
          ANTHROPIC_AUTH_TOKEN: ${{ secrets.DEEPSEEK_API_KEY }}
        run: python scripts/harness/run.py fix

  round-3:
    name: "Round 3: verify images"
    needs: fix-2
    if: always() && !failure()
    strategy:
      matrix:
        app: ${{ fromJSON(needs.query.outputs.updates) }}
    uses: ./.github/workflows/_upgrade_versions.yml
    with:
      app: ${{ matrix.app.name }}
      app_version: ${{ matrix.app.version }}
      os_version: ${{ matrix.app.os_version }}
      scenario: version-update
      retry: true
  # --- End per-app verify + fix ---

  compose-pr:
    name: Compose and create PR
    needs: [round-1, round-2, round-3]
    if: always() && !failure()
    runs-on: [self-hosted, Linux, x64]
    steps:
      - name: Check out workflow source
        uses: actions/checkout@v4
      - name: Download test results
        uses: actions/download-artifact@v4
        with: { pattern: test-results-*, merge-multiple: true }
      - name: Gate diff on changed files
        run: python scripts/harness/gate_diff.py
      - name: Compose PR title and body
        env:
          GITCODE_TOKEN: ${{ secrets.GITCODE_TOKEN }}
        run: python scripts/harness/compose_pr.py --scenario version-update
      - name: Create PR on GitCode
        env:
          GITCODE_TOKEN: ${{ secrets.GITCODE_TOKEN }}
        run: |
          python scripts/utils/gitcode.py pr create \
            --title "$(cat /tmp/pr-title)" \
            --body-file /tmp/pr-body.md \
            --head "$(git branch --show-current)"