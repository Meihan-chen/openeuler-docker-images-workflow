name: Legacy openEuler Upgrade Entry (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Use the phase-one activity workflow
    runs-on: ubuntu-latest
    steps:
      - name: Explain the supported entry
        run: |
          echo "This legacy batch workflow is disabled."
          echo "Use monitor_oe_upgrade_issues.yml with a tracking Issue."
          exit 2
